{
  "name": "personal-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal professional site, deployed to AWS by personal-site-automation",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "format": "prettier --write \"**/*.{js,jsx,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{js,jsx,json,css,md}\"",
    "lint": "eslint . --ext .js,.jsx",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "gatsby": "^5.14.0",
    "gatsby-plugin-sitemap": "^6.14.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-plugin-react": "^7.37.2",
    "prettier": "^3.4.2"
  }
}
